---
footer: true
header: true
layout: templates/base.html
title: What's new in Helperbird
youtubeId: vwT8SAJfU3E
cardTitle: What's new in Helperbird
description: See what's new in Helperbird. Browse our complete changelog with new features, bug fixes, and upcoming improvements for each version.
keywords: Helperbird updates, Helperbird changelog, what's new Helperbird, Helperbird features, dyslexia software updates, accessibility tool updates, Chrome extension updates
url: updates/
redirect_from: [/updates/robin/, /updates/102-5-3/, /updates/102-7-0/, /updates/102-7-1/]
---

<style>
  .update-card {
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .update-card:hover {
    box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
  }
</style>

<div class="px-6 py-20 sm:py-24 lg:px-8">
  <div class="mx-auto max-w-4xl text-center">
    <h1 class="text-4xl font-semibold tracking-tight text-gray-900 sm:text-6xl">
      What's new in Helperbird
    </h1>
    <p class="mt-6 text-xl leading-relaxed text-gray-600 sm:text-2xl">
      Stay up-to-date with the latest Helperbird developments right here.
    </p>
  </div>
</div>

<div class="mx-auto max-w-7xl px-6 py-12 sm:px-8 sm:py-16 lg:px-12 lg:py-20">
  {%- for update in updates.new -%}
    <article 
      id="{{ update.version }}" 
      aria-labelledby="{{ update.version }}-heading" 
      class="mb-24 md:flex md:gap-12"
    >
      <header class="mb-8 md:mb-0 md:w-1/4">
        <h2 id="{{ update.version }}-heading" class="text-lg font-semibold text-gray-900">
          Version
        </h2>
        <a 
          class="badge badge-info mt-2 inline-block" 
          href="#{{ update.version }}" 
          title="Learn more about Helperbird version {{ update.version }} updates and features"
          aria-label="Version {{ update.version }}"
        >
          {{ update.version }}
        </a>

        <h3 class="mt-6 text-lg font-semibold text-gray-900">
          Release Date
        </h3>
        <span class="badge badge-neutral mt-2 inline-block">
          {{ update.date }}
        </span>
      </header>
      
      <div class="md:w-3/4">
        <div class="update-card rounded-2xl bg-white p-8 shadow-sm ring-1 ring-gray-200">
          {%- if update.changes and update.changes.size > 0 -%}
          <section aria-labelledby="{{ update.version }}-changes">
            <h3 id="{{ update.version }}-changes" class="mb-8 text-2xl font-semibold text-gray-900">
              Changes
            </h3>
            <ul role="list" class="space-y-6">
              {%- for change in update.changes -%}
                <li class="flex items-start gap-4">
                  <span class="badge badge-secondary mt-1 flex-shrink-0">New</span>
                  <div>
                    <h4 class="font-semibold text-gray-900">{{ change.title }}</h4>
                    <p class="mt-1 text-base text-gray-600">{{ change.description }}</p>
                  </div>
                </li>
              {%- endfor -%}
            </ul>
          </section>
          {%- endif -%}

          {%- if update.fixes and update.fixes.size > 0 -%}
          <section aria-labelledby="{{ update.version }}-fixes" class="mt-12">
            <h3 id="{{ update.version }}-fixes" class="mb-8 text-2xl font-semibold text-gray-900">
              Fixes
            </h3>
            <ul role="list" class="space-y-6">
              {%- for fix in update.fixes -%}
                <li class="flex items-start gap-4">
                  <span class="badge badge-primary mt-1 flex-shrink-0">Fix</span>
                  <div>
                    <h4 class="font-semibold text-gray-900">{{ fix.title }}</h4>
                    <p class="mt-1 text-base text-gray-600">{{ fix.description }}</p>
                  </div>
                </li>
              {%- endfor -%}
            </ul>
          </section>
          {%- endif -%}
          
          {%- if update.upcoming and update.upcoming.size > 0 -%}
          <section aria-labelledby="{{ update.version }}-upcoming" class="mt-12">
            <h3 id="{{ update.version }}-upcoming" class="mb-8 text-2xl font-semibold text-gray-900">
              Upcoming
            </h3>
            <ul role="list" class="space-y-6">
              {%- for upcoming in update.upcoming -%}
                <li class="flex items-start gap-4">
                  <span class="badge badge-warning mt-1 flex-shrink-0">Upcoming</span>
                  <div>
                    <h4 class="font-semibold text-gray-900">{{ upcoming.title }}</h4>
                    <p class="mt-1 text-base text-gray-600">{{ upcoming.description }}</p>
                  </div>
                </li>
              {%- endfor -%}
            </ul>
          </section>
          {%- endif -%}
        </div>
      </div>
    </article>
  {%- endfor -%}
</div>