- var theGreat = '<script src="https://assets.helperbird.com/widget/widget.js"></script>';
- var widget = '<helperbird-widget></helperbird-widget>';
- var widgetCus = '<helperbird-widget color="#ebebeb" style="round || square" logo="url" position="right || left" icon="wheelchair || standard"></helperbird-widget>';

.bg-white#howToInstall
  .max-w-7xl.mx-auto.py-16.px-4(class='sm:px-6 lg:py-20 lg:px-8')
    div(class='lg:grid lg:grid-cols-3 lg:gap-8')
      div
        h2.text-3xl.font-extrabold.text-gray-900
          | Installing the widget
        p.mt-4.text-lg.text-gray-500
          | Can&rsquo;t find the answer you&rsquo;re looking for? Reach out to our 
          a.font-medium.text-indigo-600(href='/support' class='hover:text-indigo-500') customer support
          |  team.
      .mt-12(class='lg:mt-0 lg:col-span-2')
        dl.space-y-12
          div
            dt.text-lg.leading-6.font-medium.text-gray-900
              | Install the following script to your webpage.
            dd.mt-2
              code.html #{theGreat}

          div
            dt.text-lg.leading-6.font-medium.text-gray-900
              | Then near the bottom of website, add the following
            dd.mt-2
              code.html #{widget}
          div
            dt.text-lg.leading-6.font-medium.text-gray-900
              | You can also customize the widget.
            dd.mt-2
              code.html #{widgetCus}

          div
            dt.text-lg.leading-6.font-medium.text-gray-900
              | How do I change the color?
            dd.mt-2.text-base.text-gray-500
              | Just add the following to the widget code 'color="#ebebeb"'
          div
            dt.text-lg.leading-6.font-medium.text-gray-900
              | How do I set the widget to be left or right?
            dd.mt-2.text-base.text-gray-500
              | Just add the following to the widget code position="right" or position="left"
          div
            dt.text-lg.leading-6.font-medium.text-gray-900
              | How do I change the icon?
            dd.mt-2.text-base.text-gray-500
              | Just add the following to the widget code icon="wheelchair" or icon="standard"
          div
            dt.text-lg.leading-6.font-medium.text-gray-900
              | How do I change the widget to be square or round?
            dd.mt-2.text-base.text-gray-500
              | Just add the following to the widget code style="round" or style="square"
